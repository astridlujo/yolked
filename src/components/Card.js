import React from 'react';
import { TouchableOpacity, View, Text, StyleSheet, BackHandler } from 'react-native';

const Card = props => {
    return (
        <View style={styles.cardContainer}>
            
        </View>
    );
};

const styles = StyleSheet.create({

    cardContainer: {
        width: '45%',
        height: '40%',
        margin: 15,
        borderRadius: 10,
        backgroundColor: 'powderblue'
    },

    cardItem: {
       

    }
});

export default Card;